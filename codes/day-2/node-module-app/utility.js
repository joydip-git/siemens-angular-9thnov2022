const add = (a, b) => a + b
const subtract = (a, b) => a - b

const exportableObj = {
    // addFn: add,
    // subFn: subtract

    // add: add,
    // subtract: subtract
    add,
    subtract
}

module.exports = exportableObj